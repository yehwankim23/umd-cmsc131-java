/**
 * L3 assignment
 * 
 * Write code for the methods in this file during the lab and submit by the due 
 * Mar. 8 11:00PM. No late submission due is given for this assignment. 
 * 
 * @author iyoon
 * 
 */
class Functions {

    public static void main(String[] args) {
        System.out.println("Hello, World");     
    }

    /** 
     * Add two integers given as the parameters and return the sum 
     * 
     * @param a, b
     *         - the two integers 
     * @return the sum  
     */
    static int sumTwoInts(int a, int b) {
        throw new UnsupportedOperationException("replace with your implementation");
    }


    /** 
     * Create a new array and return it. 
     * Each element of the new array of size n must have the value 2*i 
     * where i is the index. For example, if n = 4, the new array should be
     * [0, 2, 4, 6] 
     * 
     * @param n
     *         - the array size
     * @return the array newly created  
     */
    static int[] doubleArray(int n) {
        throw new UnsupportedOperationException("replace with your implementation");
    }

    /** 
     * Return the count of unique values in two parameters. 
     *  
     * @param a, b
     *            - two integers
     * @return the count of unique values 
     */
    static int countUnique(int a, int b) {
        throw new UnsupportedOperationException("replace with your implementation");
    }

    /** 
     * Return the count of unique values in three parameters. 
     *  
     * @param a, b, c
     *            - three integers
     * @return the count of unique values 
     */
    static int countUnique(int a, int b, int c) {
        throw new UnsupportedOperationException("replace with your implementation");
    }

    /**
     * Fill in the blanks so that this code counts the number of factors of the
     * variable n that are less than n. For example, 6 has three factors (1, 2
     * and 3) and 12 has five (1,2,3,4 and 6).
     * 
     * Remember that % is the reminder operator. For example, 7 % 4 == 3. You
     * may find that operator useful.
     * 
     * @param n
     *            - number who's factors should be counted
     * @return the number of factors of n
     */
    static int countFactors(int n) {
        throw new UnsupportedOperationException("replace with your implementation");
    }

}
